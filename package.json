{
  "name": "action-deploy-theme",
  "version": "2.0.4",
  "license": "MIT",
  "author": "Ghost Foundation",
  "repository": "git@github.com:TryGhost/action-deploy-theme.git",
  "files": [
    "dist",
    "action.yml"
  ],
  "main": "dist/index.js",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "oxlint --quiet && oxfmt --check .",
    "lint:fix": "oxlint --fix && oxfmt .",
    "format": "oxfmt .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit && tsc --noEmit --project tsconfig.test.json",
    "preship": "pnpm typecheck && pnpm lint && pnpm test:coverage && pnpm build",
    "ship": "pro-ship"
  },
  "dependencies": {
    "@actions/core": "3.0.1",
    "@actions/exec": "3.0.0",
    "@tryghost/admin-api": "1.14.12",
    "@vercel/ncc": "0.45.0",
    "slug": "12.0.1"
  },
  "devDependencies": {
    "@tryghost/pro-ship": "1.1.9",
    "@types/node": "25.9.8",
    "@types/slug": "5.0.9",
    "@vitest/coverage-v8": "5.0.1",
    "oxfmt": "0.68.0",
    "oxlint": "1.83.0",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  },
  "packageManager": "pnpm@12.5.1"
}
